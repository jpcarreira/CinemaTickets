struct MovieApiData {
    
    let movieId: Int
    
    let title: String
    
    let synopsis: String?
    
    // for the sake of simplicity we keep it as an array of strings
    let actors: [String]
}
